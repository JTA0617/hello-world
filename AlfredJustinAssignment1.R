first <- readline(prompt = "Enter the first number: ")
second <-  readline(prompt = "Enter the second number: ")

    first <- as.integer(20)
    second <- as.integer(21)
result = first + second

print(paste("The sum of ",first, "and ",second, " is ", result))
